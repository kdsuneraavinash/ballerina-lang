NAME
       ballerina-run-kernel - Run iballerina jupyter kernel

SYNOPSIS
       bal run-kernel [-f|--file] <file-name>

DESCRIPTION
       Shell runs a IBallerina kernel instance that can be connected with
       a Jupyter Notebook Client using a connection file.
       Please note that this is an experimental feature.

OPTIONS
       --file
           Connection file to use.

EXAMPLES
       Run iballerina kernel.
          $ bal run-kernel -f filename
